import { Module } from '@nestjs/common';

/**
 * Laravel domain module (placeholder).
 * Suporte para projetos Laravel estará disponível em breve.
 * Quando implementado, fornecerá IModuleRegistry, IEntityIntrospector, etc.
 * equivalentes para o ecossistema Laravel (models, controllers, etc.).
 * Detecção via composer.json (laravel/framework).
 */
@Module({
  imports: [],
  providers: [],
  exports: [],
})
export class LaravelDomainModule {}
